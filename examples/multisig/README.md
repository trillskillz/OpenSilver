# MultiSig example

Planned example flow:

1. Deploy with threshold `2` and three configured signers.
2. Spend from the contract with two valid signer approvals.
3. Reconfigure the signer set using the current quorum.
4. Reuse the policy inside a Vault or Social Recovery pattern.
