# Ownable example

Planned example flow:

1. Deploy the covenant with an initial owner hash.
2. Spend through the `transfer` transition to rotate to a new owner hash.
3. Reuse the same policy inside a higher-level Vault or token-controller pattern.
